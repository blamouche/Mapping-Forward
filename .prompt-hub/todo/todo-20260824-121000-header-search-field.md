# Task: Add a search field in the site header

## Context
- Static site generated by `docs/build_site.py` (pure Python stdlib, relative links, sober design).
- README documents "Aucun JavaScript" — a functional search on static hosting (GitHub Pages) requires JS.
- Decision (approved plan): minimal vanilla JS (~1KB, no deps), dedicated lean `search-index.json` with relative URLs.

## Plan
1. Navbar: add search input + results dropdown in `page_html`; inject `window.SITE_BASE` + load `search.js` in head.
2. CSS: `.nav-search`, `#site-search`, `.search-results` (sober design), responsive at 768px.
3. Generator: write `search-index.json` (title, url, date, keywords, elevator_pitch — relative url) + `search.js` to dist.
4. README: update "Aucun JavaScript" note.
5. Rebuild, verify locally (http.server), commit + push.

## Done
- [x] Navbar + head updated
- [x] CSS added
- [x] search-index.json + search.js generated
- [x] README note updated
- [x] Rebuilt + verified locally

## Review
- Search field appears on all pages (shared navbar).
- JS minimal, vanilla, graceful degradation (hides field if index fetch fails).
- Verified: build OK (783 articles), search.js 2.1K + search-index.json 418K in dist, base injection correct ("" root / "../../" articles), JS syntax + matching logic tested with node (multi-token AND, no-match case), files served 200 via http.server.
- 2026-08-24 12:30 CEST: committed + pushed (auto-redeploy Pages).
